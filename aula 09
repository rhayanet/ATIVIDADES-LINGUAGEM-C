#include <stdio.h>

#include <stdio.h>
#include <stdlib.h> 
#include <time.h>

int main(void) {
  int num;
  srand(time(0));
  num = rand() % 50;
  printf("Numero sorteado: %d\n", num);
  return 0;
}
